<Window x:Class="ListBrowser.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="確認" Height="160" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageText" Grid.Row="0"
                   FontSize="13" Foreground="#2C3E50"
                   TextWrapping="Wrap" VerticalAlignment="Center"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="キャンセル" Click="Cancel_Click"
                    Width="90" Padding="0,7"
                    Background="#95A5A6" Foreground="White" BorderThickness="0"
                    Cursor="Hand" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="OK" Click="Ok_Click"
                    Width="90" Padding="0,7"
                    Background="#c0392b" Foreground="White" BorderThickness="0"
                    Cursor="Hand" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
